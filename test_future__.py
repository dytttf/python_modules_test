#coding:utf8
'''
from __future__ import unicode_literals, \ # 3.0加入 未指定编码的字符默认为 unicode
     division, \ # 3.0加入 支持整数相除保留小数
     print_function, \ # 3.0加入 限制 print 为函数，不能再使用 pring ''
     generators, \ # 2.3加入
     nested_scopes, \ # 2.2加入
     absolute_import, \ # 3.0加入 
     with_statement # 2.6加入 支持 with 语法

from __future__ import unicode_literals, \
     division, \
     print_function, \
     generators, \
     nested_scopes, \
     absolute_import, \
     with_statement
'''
from __future__ import (unicode_literals,
     division,
     print_function,
     generators,
     nested_scopes,
     absolute_import,
     with_statement)
#print 'hello'
#print type('hello')
#print 1/2

print('hello')
print(type('hello'))
print(1/2)

